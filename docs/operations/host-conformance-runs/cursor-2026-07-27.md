# Cursor host-behavior diagnostic — 2026-07-27

**Run status:** `NOT EXECUTED`

**Behavior verdict:** `NOT EXECUTED`

**Security verdict:** `NOT EVALUATED`

## Availability check

- Host: Cursor
- CLI lookup: `cursor-agent=NOT_FOUND`; Cursor launcher is present outside PATH.
- Desktop installation: present at `C:\Users\littl\AppData\Local\Programs\cursor\Cursor.exe`
- Desktop version: `3.13.21`
- Launcher check: `...\resources\app\bin\cursor.cmd --version` returned `3.13.21`.
- Agent CLI version: `UNKNOWN`; `cursor-agent` was not found.
- Worktree: `C:\Users\littl\Documents\AI Booster Kit\.worktrees\gate-1-research`
- Check performed: read-only command lookup, explicit Cursor launcher version check, and WSL availability check.
- Result: Cursor Desktop is installed, but the Cursor Agent CLI required for a non-interactive behavior run is not available; WSL is not installed.

## Evidence boundary

- No Cursor behavior task was started because only the desktop GUI and a launcher were available; GUI control is not exposed in the current execution environment.
- No Cursor instruction source, Project Rule, AGENTS.md loading, Team Rule, tool, memory, hook, plugin, or startup event was observed.
- No repository file, host configuration, credential, permission, or external source state changed.
- The absence of a local CLI is an execution blocker, not a Cursor behavior result.

## Team-run handoff

Run the [frozen host-behavior diagnostic task](../host-behavior-diagnostic.md) in a normal Cursor environment using either the Cursor Desktop GUI with a human operator or the Cursor Agent CLI in WSL. Use:

- host adapter: `docs/operations/host-adapters/cursor.md`;
- the native Cursor rules/instruction surface available to that team;
- the exact response sections and separate behavior/security verdicts from the protocol;
- this file as the evidence destination, preserving the complete response and host event log.

Do not fill this record with a guessed Cursor version or inferred rule-loading result.
