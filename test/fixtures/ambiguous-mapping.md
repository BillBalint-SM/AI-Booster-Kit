# Ambiguous local mapping fixture

This fixture represents two configured candidates with the same requested stable
keys. It is intentionally local-only and contains no live tenant, space, or
repository values.
